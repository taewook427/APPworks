### LocalHub

> Simple HTTP server for sharing text and files in local network.

http로 서버가 열린 기기의 IP:port에 연결해 접속합니다.  Access `http://IP:port` to join the server.

기본 포트는 5000이지만, 인자 전달로 수동 설정할 수 있습니다. You can manually set port by argument (default is 5000).
